#include<stdio.h>
#include<string.h>

int main()

    {
        char s[]= "Suparna";
        int i = 0;
        
        while(s[i] != '\0') {
            printf("%c\n", s[i]);
            i++;
        }
        return 0;
    }
