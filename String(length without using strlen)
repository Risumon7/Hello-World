#include<stdio.h>
#include<string.h>
int main(void)

    {
        char s[] = "Md Rafiqul Islam";
        int i =0, len = 0;
        
        while(s[i] != '\0') {
            i++;
            len++;
        }
        
        printf("Length = %d", len);
        return 0;
    }
